To install and run simulation (requires Python3)

- pip install -r simulator/requirements.txt
- python -m simulator.main.<your main file without .py>

Charts are generated into the 'experiments' folder.